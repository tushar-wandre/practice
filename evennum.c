#include<stdio.h>
int main () {
    int n,i;
    int arr[100];
    int sum =0,max;
    printf("enter 6 number of element:");
    scanf("%d",&n);
    printf("Enter%d element:\n",n);
    for (i=0; i<n; i++){
        scanf("%d",&arr[i]);
        sum=+arr[i];

    }
    max=arr[0]
    for(i)
}